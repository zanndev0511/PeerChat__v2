import { Val } from "value-enhancer";
import type { Displayer, InvisiblePluginContext } from "white-web-sdk";
import { InvisiblePlugin } from "white-web-sdk";
import { SyncedStore } from "./synced-store";
export declare class SyncedStorePlugin extends InvisiblePlugin<any> {
    static readonly kind: string;
    static readonly invisiblePlugins: Map<Displayer<import("white-web-sdk").DisplayerCallbacks>, Val<InvisiblePlugin<any> | null, any>>;
    static init<TEventData extends Record<string, any> = any>(displayer: Displayer): Promise<SyncedStore<TEventData>>;
    static onCreate(plugin: SyncedStorePlugin): void;
    constructor(context: InvisiblePluginContext);
}
